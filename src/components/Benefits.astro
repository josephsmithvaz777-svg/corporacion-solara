---
import { Icon } from 'astro-icon/components';
import { benefits } from '../data/site';
---

<section class="section benefits" id="beneficios" aria-labelledby="beneficios-title">
  <div class="container">
    <p class="section__eyebrow">Confianza primero</p>
    <h2 class="section__title" id="beneficios-title">Más que un terreno, una oportunidad</h2>
    <p class="section__lead">
      Ayudamos a personas y emprendedores a invertir en espacios estratégicos con claridad y respaldo.
    </p>

    <div class="benefits__grid">
      {
        benefits.map((item) => (
          <article class="benefit">
            <div class="benefit__icon" aria-hidden="true">
              <Icon name={item.icon} size={28} />
            </div>
            <h3>{item.title}</h3>
            <p>{item.description}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .benefits {
    background: var(--color-surface);
  }

  .benefits__grid {
    display: grid;
    gap: 1.25rem;
    margin-top: var(--space-5);
  }

  .benefit {
    background: var(--color-primary);
    color: var(--color-white);
    border-radius: var(--radius-card);
    padding: var(--space-4);
    box-shadow: var(--shadow-soft);
    transition: transform var(--transition);
  }

  .benefit:hover {
    transform: translateY(-6px);
  }

  .benefit__icon {
    width: 48px;
    height: 48px;
    border-radius: 14px;
    display: grid;
    place-items: center;
    color: var(--color-gold);
    background: rgba(199, 154, 43, 0.12);
    margin-bottom: 1rem;
  }

  .benefit__icon :global(svg) {
    width: 28px;
    height: 28px;
  }

  .benefit h3 {
    color: var(--color-white);
    font-family: var(--font-body);
    font-size: 1.15rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .benefit p {
    color: rgba(255, 255, 255, 0.78);
    font-size: 0.95rem;
  }

  @media (min-width: 768px) {
    .benefits__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* Con sidebar del formulario (desde 1280px) mejor 2 columnas; 4 solo en pantallas muy anchas */
  @media (min-width: 1600px) {
    .benefits__grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
</style>
